{
  "name": "mvideo-promo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd webapp && npm install && npm run build && rm -rf ../backend/public && cp -R dist ../backend/public",
    "start": "cd backend && npm install && node server.js",
    "bot": "node bot.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "telegraf": "^4.16.3"
  }
}